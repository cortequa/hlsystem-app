const API = 'http://109.164.15.139:3000/api';

export const ENV = {
  API: {
    API: API,
    ENDPOINTS: {
      PRODUCTS: `${API}/products`,
      VISITORS: `${API}/visitors`,
      GATES: `${API}/gates`,
      ORDERS: `${API}/orders`,
      ENTRIES: `${API}/entries`,
    }
  }
};